#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 return (Success)
 */
int main(void)
{
	int fib1 = 1;
	int fib2 = 2;
	int i;
	int fib3;

	printf("%d, %d", fib1, fib2);
	for (i = 2; i < 98; ++i)
	{
		fib3 = fib1 + feb2
			printf(", %d", fib3);
		fib1 = fib2;
		fib2 = fib3;
	}
	printf("\n");

	return (0);
}
