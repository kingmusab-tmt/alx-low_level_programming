#include <stdio.h>
/**
 * main - Entry point
 *   
 * Return: Always 0 (Success)
 */
int main(void)
{
	char magic[] = "\"Programming is like building a multilingual puzzle";

	puts(magic);
return (0);
}
